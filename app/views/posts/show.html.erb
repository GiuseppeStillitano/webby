<!--pagina dove viene visualizzato il post attraverso le funzioni date in (post_controller.rb)-->

<h1><%= @post.title %></h1><br>

<p><%= @post.content %></p>

<br>
<br>

<%= link_to "Edit", edit_post_path(@post) %>
<%= link_to "Delete", post_path(@post), method: :delete, data: { confirm: "sei sicuro di voler eliminare il post?"} %>

